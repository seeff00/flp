framework:
    default_locale: bg
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - bg
#        providers:
#            crowdin:
#                dsn: '%env(CROWDIN_DSN)%'
#            loco:
#                dsn: '%env(LOCO_DSN)%'
#            lokalise:
#                dsn: '%env(LOKALISE_DSN)%'
